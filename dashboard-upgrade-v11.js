/* YouTube Command Center v11 — Today panel: actionable priorities instead of another passive analytics block. */
(()=>{
  const $=id=>document.getElementById(id);
  const fmt=n=>{n=Number(n||0);if(n>=1e9)return(n/1e9).toFixed(2)+'B';if(n>=1e6)return(n/1e6).toFixed(2)+'M';if(n>=1e3)return(n/1e3).toFixed(1)+'K';return n.toLocaleString()};
  const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));
  function injectStyle(){if($('v11style'))return;const s=document.createElement('style');s.id='v11style';s.textContent=`#todayCommand{margin-top:18px}.today-head{display:flex;justify-content:space-between;gap:18px;align-items:end;margin-bottom:14px}.today-grid{display:grid;grid-template-columns:1.35fr 1fr 1fr;gap:14px}.today-card{padding:20px;min-width:0}.today-card .kicker{font-size:11px;letter-spacing:.08em;text-transform:uppercase;color:#777;font-weight:800}.today-card h3{margin:7px 0 5px;font-size:20px}.today-card p{margin:0;color:#666;font-size:13px;line-height:1.5}.today-action{display:inline-flex;margin-top:15px;padding:9px 12px;border-radius:12px;background:#111;color:#fff;text-decoration:none;font-weight:750;font-size:12px}.today-stat{font-size:30px;font-weight:850;margin:7px 0}.today-list{display:grid;gap:8px;margin-top:12px}.today-row{display:flex;align-items:center;gap:9px;padding:9px;border-radius:13px;background:rgba(255,255,255,.55);cursor:pointer}.today-row:hover{background:rgba(255,255,255,.85)}.today-row img,.today-row .avatar-fallback{width:30px;height:30px}.today-row .avatar-shell{width:30px;height:30px;flex:0 0 30px}.today-row-main{min-width:0;flex:1}.today-row-main strong{display:block;font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.today-row-main span{display:block;color:#777;font-size:10px}.today-gap{font-size:11px;font-weight:800;white-space:nowrap}.today-empty{color:#777;font-size:12px;margin-top:10px}@media(max-width:850px){.today-grid{grid-template-columns:1fr 1fr}}@media(max-width:600px){.today-head{display:block}.today-grid{grid-template-columns:1fr}}`;document.head.appendChild(s)}
  function card(c, label, body, href){return `<article class="today-card glass"><span class="kicker">${label}</span><h3>${esc(c.title)}</h3><p>${body}</p><a class="today-action" href="${href}">Open channel →</a></article>`}
  function render(){const data=window.youtubeNetworkData||[];if(!data.length||$('todayCommand'))return;injectStyle();const main=document.querySelector('main');const anchor=$('channels');if(!main||!anchor)return;const sorted=[...data].sort((a,b)=>b.subs-a.subs);const near=[...data].filter(c=>c.subs<1000).sort((a,b)=>Math.abs(1000-a.subs)-Math.abs(1000-b.subs))[0]||sorted[0];const gate500=[...data].filter(c=>c.subs<500).sort((a,b)=>b.subs-a.subs)[0]||sorted[0];const active=[...data].filter(c=>c.videos>0).sort((a,b)=>b.views-b.views||b.subs-a.subs)[0]||sorted[0];const totalSubs=data.reduce((x,c)=>x+c.subs,0);const totalVideos=data.reduce((x,c)=>x+c.videos,0);const section=document.createElement('section');section.className='section';section.id='todayCommand';section.innerHTML=`<div class="today-head"><div><p class="eyebrow">TODAY</p><h2>Your next useful move.</h2><p>Three signals from your current public channel data — not another vanity dashboard.</p></div><span class="api-badge">${data.length} channels • ${fmt(totalSubs)} subs • ${fmt(totalVideos)} videos</span></div><div class="today-grid">${card(near,'NEXT MILESTONE',near?`${fmt(Math.max(0,1000-near.subs))} subscribers remain to reach 1,000.`:'No data',near?`./channel.html?handle=${encodeURIComponent(near.handle)}`:'#')}${card(gate500,'500 TARGET',gate500?`${fmt(Math.max(0,500-gate500.subs))} subscribers remain to reach the 500 threshold.`:'No data',gate500?`./channel.html?handle=${encodeURIComponent(gate500.handle)}`:'#')}<article class="today-card glass"><span class="kicker">NETWORK</span><div class="today-stat">${fmt(totalSubs)}</div><p>Subscribers across the network. Keep the portfolio visible, but make publishing decisions at the channel level.</p><div class="today-list">${sorted.slice(0,3).map((c,i)=>`<div class="today-row" onclick="location.href='./channel.html?handle=${encodeURIComponent(c.handle)}'"><span class="today-gap">#${i+1}</span>${window.avatarMarkup?window.avatarMarkup(c,'today-avatar'):''}<div class="today-row-main"><strong>${esc(c.title)}</strong><span>${fmt(c.subs)} subscribers</span></div></div>`).join('')}</div></article></div>`;anchor.after(section)}
  function boot(){render()}window.addEventListener('youtube-data-ready',render);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
